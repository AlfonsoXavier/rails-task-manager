<h2><%= @task.title %></h2>

<div class="container">
    <ul>
      <li><%= @task.details %></li>
      <li><%= @task.completed %></li>
    </ul>
  </div>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Really? Are you sure?"}
%>

<%= link_to "Back home", tasks_path %>

<%= link_to "Edit a task", update_task_path %>
